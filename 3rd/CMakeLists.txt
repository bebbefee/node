INCLUDE_DIRECTORIES(.)

aux_source_directory(lua SRC_FILES)
LIST(REMOVE_ITEM SRC_FILES "lua/lua.c" "lua/luac.c" "lua/lbitlib.c")

ADD_LIBRARY(lua SHARED ${SRC_FILES})
SET(LIBRARY_OUTPUT_PATH ${PROJECT_SOURCE_DIR}/bin_${NODE_BUILD_TYPE})
